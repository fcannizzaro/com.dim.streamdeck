import { Action, BaseAction } from '@stream-deck-for-node/sdk';

/*
   LS / Nightfall / Wellspring Rotations
   const tiles = sd.allContexts()["com.dim.streamdeck.page"];
*/
@Action('rotations')
export class Rotations extends BaseAction {
  // WIP
}
