import { Action } from '@stream-deck-for-node/sdk';
import { BaseDimAction } from './BaseAction';

/*
   LS / Nightfall / Wellspring Rotations
   const tiles = sd.allContexts()["com.dim.streamdeck.page"];
*/
@Action('rotations')
export class Rotations extends BaseDimAction {
  // WIP
}
